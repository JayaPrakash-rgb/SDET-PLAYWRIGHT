Question 1: The Flight Booking Data Cleaner.

->.split()  ->Used to "cut" the raw string into pieces. First by : to separate price, then by - to separate cities.
->.map()    -> used to take list and transform each string into a structured Object.
->isNaN()   -> to check non a number for prices.
->.filter() ->where the price was $\ge 100$ and $\le 500$.
->.sort()   ->(a, b) => a.price - b.price for ascending order.
-> JSON.stringify Return as a JSON string .

=============================================================================================================================================
Question 2: The E-Commerce Discount Applicator.

->Callbacks  and functions.
 ->applyPromo   (isEligible).
->.map()  to new arry without modifying original cart.
->(* 0.9;) // Reduce by 10%  for transforming price.//discount.
-->   ...item)   for Used to copy all existing properties of a product before adding or updating the price and isDiscounted.
->//(Template Literal)  for  final log message.

==============================================================================================================================================
Question 3: The Movie Stream Analytics.

->split('|')     for parsing.
->isNaN()   -> to check non a number  for views.
->Logical OR (||) and .filter()for  //Filtering: Action or Sci-Fi AND views > 5000.
->.sort((a, b) => b.views - a.views);         //Descending.
->  JSON.stringify Return as a JSON string .